;;; modules/expand-region-config.el --- Expand region configuration -*- lexical-binding: t; -*-

(use-package expand-region
  :ensure t
  :commands (er/expand-region er/contract-region))

(provide 'expand-region-config)
;;; expand-region-config.el ends here
